name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: Build (iOS Simulator)
    runs-on: macos-15

    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_16.app

      - name: Build
        run: |
          xcodebuild \
            -project AtlasQuizApp/AtlasQuiz.xcodeproj \
            -scheme AtlasQuiz \
            -configuration Debug \
            -destination 'generic/platform=iOS Simulator' \
            build \
            CODE_SIGNING_ALLOWED=NO \
            | xcpretty --color || true
          # Re-run without xcpretty to capture raw exit code
          xcodebuild \
            -project AtlasQuizApp/AtlasQuiz.xcodeproj \
            -scheme AtlasQuiz \
            -configuration Debug \
            -destination 'generic/platform=iOS Simulator' \
            build \
            CODE_SIGNING_ALLOWED=NO \
            -quiet

      - name: Confirm success
        run: echo "✅ Build succeeded"
